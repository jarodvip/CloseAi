# 攻单 AI Agent 项目说明与交接文档

> 文档版本：2026-08-16  
> 项目名称：`gongdan-ai-agent`
> 产品名称：攻单 AI｜分众销售作战台  
> 当前部署域名：<https://gongdanai-h7miejwc.manus.space>  
> GitHub：<https://github.com/jarodvip/CloseAi>  
> 当前远程基线：`main` / `73db09d13e178676449ddd7e013ee1a3ab3ff025`

## 1. 项目定位

攻单 AI 是一款面向分众传媒销售团队的免登录销售作战工具。它把客户问诊、客户类型识别、攻单策略生成、异议处理、拜访复盘与后续跟进串联为一条闭环工作流，帮助销售在拜访前快速判断客户处境，在拜访中获得可执行的话术，在拜访后沉淀反馈和下一次行动。

产品的核心原则是：**先辨类，再攻单；先听懂客户，再给出推进动作。** AI 输出必须以项目内置的分众七大客户类型、攻单五步法、异议知识库、案例库和话术库为上下文，不应脱离这些方法论随意编造策略或案例。

## 2. 当前功能总览

| 功能模块 | 主要能力 | 闭环作用 |
|---|---|---|
| 客户诊断 | 通过品牌名称、行业、规模、老板关键词、竞争状态、拜访目标和补充背景逐步问诊；支持单选、多选和手动补充 | 收集情报，判断客户主类型与次类型 |
| 七大类型 | 展示品牌野心型、定位卡位型、品类开创者型、竞争突围型、资本叙事型、全国化扩张型、品牌焕新型的信号、切入点、忌讳和话术 | 把客户处境转化为作战方向 |
| 攻单助手 | 基于当前诊断上下文或销售自由输入，生成个性化攻单策略、五步法、破冰话术、核心沟通和下一步动作 | 将判断转化为拜访打法 |
| 异议速查 | 覆盖“太贵了”“再看看”“投过线上效果不好”“品牌太小”“老板不同意”等高频异议；支持带入品牌、行业、主次类型和目标生成针对性回应 | 处理阻力，推动客户继续决策 |
| 客户画像 | 根据问诊信息和 AI 结论生成发展阶段、决策驱动力、市场张力、拜访目标、画像摘要和风险提示 | 形成可复习、可交接的客户认知 |
| PDF 报告 | 导出客户画像、类型判断、判断依据、策略、五步法、案例、话术和行动计划 | 生成拜访前简报或内部汇报材料 |
| 我的记录 | 免登录保存在当前浏览器；支持状态、下次跟进日期、拜访反馈和异议处理结果回写 | 沉淀拜访结果，形成下一次行动 |
| 待办视图 | 识别逾期、今日和未来三天内到期记录，展示待办数量与快捷入口 | 管理后续跟进优先级 |
| 记录迁移 | JSON 导出与导入，按记录 ID 合并，导入记录优先、本机未冲突记录保留 | 在设备之间或团队协作前迁移数据 |

## 3. 业务闭环

完整使用路径如下：

```text
客户诊断
  ↓
识别主类型 / 次类型
  ↓
自动高亮七大类型卡与匹配案例
  ↓
一键带入攻单助手
  ↓
生成听 / 认 / 比 / 算 / 定五步打法
  ↓
遇到异议，带入品牌与客户类型生成个性化回应
  ↓
保存客户画像与攻单方案
  ↓
拜访后回写推进状态、跟进日期、客户反馈和异议结果
  ↓
自动生成下一次攻单提示，并进入待办视图
```

其中，四个核心模块的职责分别是：客户诊断负责“发现问题”，七大类型负责“解释问题”，攻单助手负责“设计动作”，异议速查负责“处理阻力”。拜访回写和待办提醒则把一次销售动作连接到下一次销售动作。

## 4. 内置业务知识库

### 4.1 七大客户类型

| 类型 | 典型关注点 | 典型切入方向 |
|---|---|---|
| 品牌野心型 | 希望成为行业头部、提升品牌势能 | 建立全国性品牌心智与长期声量 |
| 定位卡位型 | 需要抢占细分定位或关键心智 | 先卡位，再建立品类联想 |
| 品类开创者型 | 正在教育市场、创造新品类 | 用高频触达降低用户理解成本 |
| 竞争突围型 | 面对强竞品和同质化竞争 | 找到竞争差异并放大品牌势能 |
| 资本叙事型 | 有融资、上市或估值增长诉求 | 把品牌增长转化为资本可理解的长期资产 |
| 全国化扩张型 | 从区域走向全国或拓展新市场 | 统一全国认知，支撑渠道与市场扩张 |
| 品牌焕新型 | 老品牌、老产品或老认知需要更新 | 重建年轻用户心智与新的品牌表达 |

### 4.2 攻单五步法

“听”用于听清客户现状、目标和真实顾虑；“认”用于确认客户已经意识到的问题；“比”用于建立与竞品、行业或增长目标之间的比较；“算”用于把投放价值、增长机会和风险转化为可讨论的账；“定”用于确认下一步动作、时间和决策参与人。

### 4.3 案例库与自动匹配

项目内置飞鹤、妙可蓝多、元气森林、Ulike、波司登等标杆案例。案例不是由模型自由编造，而是由客户主类型和次类型通过程序化匹配函数从内置案例库中选择，再作为诊断结果的佐证。

### 4.4 异议处理库

当前内置高频异议包括价格敏感、暂不决策、线上投放效果不佳、品牌规模较小和老板未同意。处理方式遵循“先确认真实顾虑，再给低风险下一步”的原则，不直接与客户争辩。

## 5. 技术架构

| 层级 | 技术与职责 |
|---|---|
| 前端 | React 19、Vite、TypeScript、Tailwind CSS 4、shadcn/ui 风格组件、Lucide 图标、Wouter 路由 |
| 交互与数据 | React state、TanStack Query/tRPC hooks、浏览器 `localStorage` |
| 服务端 | Express 4、tRPC 11、Manus 项目运行时 |
| AI | 服务端通过项目内置 LLM 能力调用模型，并把知识库拼接为系统上下文 |
| 数据库 | Drizzle ORM、MySQL/TiDB；保留用户与服务端记录能力，但当前免登录工作流主要使用本地记录 |
| 文件与报告 | 浏览器端生成 PDF；客户记录通过 JSON 文件导入导出 |
| 测试 | Vitest；另有一次性浏览器端到端脚本验证关键流程 |
| 部署 | Manus WebDev 托管，当前使用 Autoscale 模式 |

## 6. 主要目录

```text
client/
  index.html                         页面入口与产品元信息
  src/
    App.tsx                           路由与主题入口
    pages/Home.tsx                    核心销售工作台
    components/AIChatBox.tsx          AI 对话组件
    components/DashboardLayout.tsx    工作台布局组件
    components/ui/                    shadcn/ui 风格基础组件
    lib/customerProfile.ts            客户画像确定性生成逻辑
    lib/localRecords.ts               本地记录、提醒、摘要、导入导出
    index.css                         全局设计 token 与样式

server/
  routers.ts                          tRPC 路由、诊断、AI 对话和知识接口
  db.ts                               数据库连接与查询 helpers
  storage.ts                          文件存储封装
  _core/                              Manus 运行时、认证、LLM、OAuth 等基础设施
  *.test.ts                           Vitest 测试

drizzle/
  schema.ts                           数据表定义
  migrations/                         Drizzle 迁移文件

shared/
  gongdanKnowledge.ts                 七大类型、五步法、案例、异议和匹配逻辑
  types.ts                            跨前后端共享类型

docs/
  攻单AI-Agent使用说明.md             销售人员使用手册
  攻单AI-Agent-API文档.md              开发者 API 与数据契约

todo.md                               项目功能与验证历史
RENAME.MD                             本项目交接、维护与现状说明
package.json                          依赖与运行脚本
vite.config.ts                        Vite 配置
vitest.config.ts                      Vitest 配置
```

## 7. 数据流与存储边界

### 7.1 客户诊断与 AI 输出

销售在问诊页面输入客户品牌和销售线索。前端将问诊结果通过 tRPC 诊断接口发送到服务端；服务端进行输入校验，将问诊信息、七大类型知识、五步法、异议库和案例库组织为 AI 上下文，再返回结构化攻单结果。案例名称由程序化匹配逻辑约束，避免模型自由生成不存在的案例。

### 7.2 客户画像

客户画像由 `client/src/lib/customerProfile.ts` 的 `buildCustomerProfile` 基于问诊输入和 Playbook 确定性生成，不依赖第二次模型调用。画像包括品牌名称、客户阶段、决策驱动力、市场张力、拜访目标、摘要和风险相关提示。

### 7.3 本地记录

免登录记录使用浏览器键名 `gongdan-ai-local-records`。记录包括品牌名称、主类型、次类型、诊断、Playbook、创建时间、推进状态、下次跟进日期、拜访反馈和异议处理结果。清除浏览器站点数据会导致本机记录丢失，因此重要记录应及时导出 JSON。

### 7.4 JSON 迁移

导出格式如下：

```json
{
  "version": 1,
  "exportedAt": "2026-08-16T00:00:00.000Z",
  "records": []
}
```

导入会校验必要字段，并按 `id` 去重合并。当前策略是导入记录优先，已有本机记录中未冲突的记录继续保留。导入不是云端同步，也不会自动上传团队服务器。

### 7.5 PDF 报告

PDF 在浏览器端生成，不依赖服务端文件存储。报告内容应包含品牌名称、客户画像、主次类型、判断依据、攻单策略、五步法、案例、话术和行动计划。报告下载失败时，优先检查浏览器下载权限、弹窗策略和客户端运行时错误。

## 8. 开发与验证命令

在项目根目录执行：

```bash
pnpm install
pnpm dev
pnpm check
pnpm test
pnpm build
```

常用结果含义如下：`pnpm check` 执行 TypeScript 静态检查；`pnpm test` 执行 Vitest；`pnpm build` 执行前端构建和服务端打包；`pnpm dev` 启动本地开发服务。

项目中还存在一次性浏览器验证脚本，例如 `loop_e2e.mjs`，用于检查诊断上下文、异议上下文、拜访回写、待办视图和导出提示。此类脚本依赖本地 Chromium 与运行环境，不应作为线上运行时依赖。

## 9. 测试覆盖

当前测试覆盖知识库完整性、案例匹配、客户画像生成、诊断与记录路由、认证退出、本地记录恢复、跟进提醒、反馈摘要、JSON 导入导出等内容。最近一次完整测试结果为 **5 个测试文件、11 个测试用例全部通过**；闭环端到端验证结果为：`assistantContext=true`、`objectionContext=true`、`feedbackSaved=true`、`dueVisible=true`、`exportNotice=true`。

视觉验证应至少覆盖桌面端和移动端的客户诊断、品牌输入、记录待办和导入导出入口。对 PDF 导出还应进行浏览器端有效 PDF Blob 或 `%PDF` 文件头验证。

## 10. 部署与版本管理

项目由 Manus WebDev 管理，成功创建 checkpoint 后会形成可预览版本；当前产品已部署到 `gongdanai-h7miejwc.manus.space`。GitHub 远程仓库为 `jarodvip/CloseAi`，主分支为 `main`。提交前应确认 `git status` 干净，推送后应核对本地 HEAD 与 `git ls-remote` 返回的远程哈希一致。

建议的版本流程是：先完成代码和测试，再阅读 `todo.md` 确认任务状态；随后创建 checkpoint；最后执行 Git 提交和推送。不要把 `node_modules`、运行日志、浏览器下载物、临时端到端产物和本地环境变量提交到仓库。

## 11. 安全与隐私边界

当前产品免登录，记录默认保存在使用者当前浏览器，不提供跨设备自动同步。品牌名称、客户背景、拜访反馈和异议结果可能包含商业敏感信息，销售人员应避免在公共设备上留下记录，并在共享前检查 PDF 和 JSON 文件内容。

AI 输出应被视为销售辅助建议，而不是事实证明或合同承诺。案例、客户数据、预算和效果数字必须以内部已确认资料为准；不得将模型生成内容直接当作客户事实对外发送。

## 12. 当前已知事项与维护建议

当前交接检查发现，项目历史 checkpoint 的说明曾记录 TypeScript 检查通过，但开发服务日志中仍出现 `Home.tsx` 将 `FormState` 传给需要 `brandName` 的 `CustomerProfileInput` 的类型错误。维护者在下一次功能迭代前应重新执行 `pnpm check`，并核对 `FormState`、`CustomerProfileInput` 与所有 `buildCustomerProfile` 调用点，确保代码基线和 checkpoint 说明一致。

后续维护优先级建议如下：

| 优先级 | 建议 | 目的 |
|---|---|---|
| P0 | 修复并重新验证 Home.tsx 的品牌字段类型契约 | 消除开发服务的类型错误，避免构建回归 |
| P1 | 增加记录导入前的预览、冲突数量和撤销操作 | 降低团队迁移误操作风险 |
| P1 | 增加跟进提醒的浏览器通知或日历导出 | 提高待办执行率 |
| P2 | 增加可选云端同步与团队权限 | 支持跨设备、跨销售协作 |
| P2 | 增加更多行业案例和真实异议样本 | 提高类型判断与话术贴合度 |

## 13. 相关文档

项目内已有两份配套文档：

- [攻单 AI Agent 使用说明](docs/攻单AI-Agent使用说明.md)
- [攻单 AI Agent API 文档](docs/攻单AI-Agent-API文档.md)

本文件用于项目交接和维护总览；销售人员应优先阅读使用说明，开发者和集成方应优先阅读 API 文档。

## 14. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 新增本项目总览、产品闭环、技术架构、数据流、部署、测试、隐私边界与维护建议 |
| 2026-08-16 | 纳入客户品牌分析、客户画像、PDF 报告、异议上下文、拜访回写、待办提醒和 JSON 迁移能力 |
